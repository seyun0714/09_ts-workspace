export {};

/*
  📌 변수 선언 및 초기화 
  1. 타입스크립트에서는 변수 선언 시 타입을 명시적으로 지정해야 함
  2. 작성법
    let|const 변수명: 타입 = 값;
  3. 타입을 명시하지 않을 경우 유의사항 
    1) 변수 선언 시    : any 타입으로 추론됨
    2) 특정 값 할당 시 : 해당 값의 타입으로 추론됨
*/

let num: number; // number 타입으로 명시적 선언
num = 10; // 올바른 할당
// num = 'hello'; // 오류: 'string' 타입은 'number' 타입에 할당할 수 없음

let str: string;
str = `hello ${num}`; // 올바른 할당
// str = 20; // 오류: 'number' 타입은 'string' 타입에 할당할 수 없음
str = '';
// str = null; // 오류: 'null' 타입은 'string' 타입에 할당할 수 없음
// str = undefined; // 오류: 'undefined' 타입은 'string' 타입에 할당할 수 없음

let bool: boolean;
bool = true; // 올바른 할당
// bool = 1; // 오류: 'number' 타입은 'boolean' 타입에 할당할 수 없음

let nul: null;
nul = null; // 올바른 할당
// nul = undefined; // 오류: 'undefined' 타입은 'null' 타입에 할당할 수 없음
// nul = 0; // 오류: 'number' 타입은 'null' 타입에 할당할 수 없음

let undef: undefined;
undef = undefined; // 올바른 할당
// undef = null; // 오류: 'null' 타입은 'undefined' 타입에 할당할 수 없음
// undef = ''; // 오류: 'string' 타입은 'undefined' 타입에 할당할 수 없음

let big: bigint;
big = 123456789012345678901234356342525432543252354325243n; // 올바른 할당
// big = 10; // 오류: 'number' 타입은 'bigint' 타입에 할당할 수 없음
console.log(big);

let small: number = 123456789012345678901234356342525432543252354325243; // number 타입으로 표현 가능한 최대 정밀도 초과
console.log(small);

let sym: symbol; // 고유하고 변경 불가능한 원시 타입
sym = Symbol('id'); // 올바른 할당
// sym = 'id'; // 오류: 'string' 타입은 'symbol' 타입에 할당할 수 없음

/*
  📌 기본 타입 (Primitive Types)
  1. 원시 값을 표현하는 타입
  2. 종류
    1) number
    2) string
    3) boolean
    4) null
    5) undefined
    6) symbol
    7) bigint
*/
