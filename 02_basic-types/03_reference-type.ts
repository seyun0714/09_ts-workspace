// 참조 타입: object, array, tuple, enum, function
export {};

/*
  📌 객체 타입 
  1. 자바스크립트의 객체(원시값 제외)를 표현하는 타입
  2. 종류
    1) object : 타입 단순 지정
    2) {속성: 타입, ..} : 객체 리터럴 타입 지정
*/

// 1) object: 모든 객체 타입 가능, 원시 타입 불가
let obj1: object; // object 타입으로 명시적 선언
obj1 = { name: '홍길동' };
obj1 = [1, 2, 3];
obj1 = new Date();
// obj1 = 10; // 오류: 'number' 타입은 'object' 타입에 할당할 수 없음

// 2) {}: 객체 리터럴 타입
let obj2: { name: string; age: number }; // 객체 리터럴 타입으로 명시적 선언
obj2 = { name: '홍길동', age: 30 }; // 올바른 할당
// obj2 = { name: '홍길동' }; // 오류: 'age' 프로퍼티가 누락됨
// obj2 = { name: '홍길동', age: '30' }; // 오류: 'age' 프로퍼티는 'number' 타입이어야 함
// obj2 = { name: '홍길동', age: 30, city: 'Seoul' }; // 오류: 'city' 프로퍼티는 선언되지 않음

/*
  📌 배열 타입 (Array Types)
  1. 자바스크립트의 배열을 표현하는 타입
  2. 종류
    1) [] : 타입 단순 지정
    2) Array<타입> :제네릭 타입 Array<> 사용
*/

// 1) []: 타입 단순 지정 => 일반적
let arr1: number[]; // number 타입의 배열로 명시적 선언
arr1 = [1, 2, 3]; // 올바른 할당
// arr1 = [1, '2', 3]; // 오류: 'string' 타입은 'number' 타입에 할당할 수 없음
// arr1 = 'hello'; // 오류: 'string' 타입은 'number[]' 타입에 할당할 수 없음

let arr2: string[]; // string 타입의 배열로 명시적 선언
arr2 = ['a', 'b', 'c']; // 올바른 할당
// arr2 = ['a', 2, 'c']; // 오류: 'number' 타입은 'string' 타입에 할당할 수 없음

// 2) Array<타입>: 제네릭 타입 Array<> 사용 => 덜 일반적
let arr3: Array<number> = [1, 2, 3]; // boolean 타입의 배열로 명시적 선언
let arr4: Array<string> = ['a', 'b', 'c']; // string 타입의 배열로 명시적 선언

// 번외) 객체 배열
let users: { name: string; age: number }[];
users = [
  { name: '홍길동', age: 30 },
  { name: '김철수', age: 25 },
];
// users = [ { name: '홍길동', age: '30' } ]; // 오류: 'age' 프로퍼티는 'number' 타입이어야 함

/*
  📌 튜플 타입 (Tuple Types)
  1. 고정 길이, 각 요소마다의 타입을 지정 할 때 사용
  2. 작성법
    [타입, 타입, ..]
  3. 유의사항
    튜플 타입 지정 시 각 요소의 순서와 타입이 중요
*/

let tuple1: [number, string, boolean];
tuple1 = [1, 'hello', true]; // 올바른 할당
// tuple = ['hello', 1, true]; // 오류: 순서 및 타입 불일치
// tuple = [1, 'hello']; // 오류: 요소 개수 불일치
// tuple = [1, 'hello', true, 42]; // 오류: 요소 개수 불일치

let tuple2: [string, number] = ['age', 30]; // 선언과 동시에 초기화 가능
